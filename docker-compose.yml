version: '3.8'
services:
  web:
    build: .
    image: hng-task-app:latest
    container_name: hng_task_container
    restart: always
    # CRITICAL FIX: Map host port 8080 to container port 80 (where Nginx Alpine runs)
    ports:
      - "8080:80"
